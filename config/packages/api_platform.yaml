api_platform:
    title: 'Banking & Insurance API'
    description: 'API pour système Banque/Assurance - Test hydrateur'
    version: '1.0.0'
    formats:
        jsonld: ['application/ld+json']
        json: ['application/json']
    enable_swagger_ui: true
    mapping:
        paths: []  # Disable automatic entity mapping to avoid access level issues
